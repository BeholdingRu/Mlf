# MLF

Веб-кабинет привычек на React + Supabase лежит в папке [`web`](web/README.md).

```
cd web
copy .env.example .env
# заполните VITE_SUPABASE_URL и VITE_SUPABASE_ANON_KEY
npm install
npm run dev
```

SQL-схема: `web/supabase/schema.sql`.
